using System;

namespace ProjectReport.Models.Geometry.Wellbore
{
    /// <summary>
    /// Backwards-compatible enum used across the Geometry module.
    /// Some files reference `WellboreSectionType` while others use `WellSectionType`.
    /// This enum provides the commonly used members needed by the codebase.
    /// </summary>
    public enum WellboreSectionType
    {
        // Common classifications used by the views/viewmodels
        Casing,
        Liner,
        OpenHole,
        Riser,
        ConductorCasing,
        SurfaceCasing,
        IntermediateCasing,
        ProductionCasing,
        CasedHole
    }
}
