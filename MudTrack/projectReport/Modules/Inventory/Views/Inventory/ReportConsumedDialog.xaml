<Window x:Class="ProjectReport.Views.Inventory.ReportConsumedDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Consumption Report" Height="220" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Consumption Report (Fluid)" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,0,0,8">
            <TextBlock Text="Fluid" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <!-- Editable ComboBox con filtrado -->
            <ComboBox x:Name="FluidCombo"
                      ItemsSource="{Binding FluidsView}"
                      DisplayMemberPath="DisplayName"
                      SelectedItem="{Binding SelectedFluid, Mode=TwoWay}"
                      Text="{Binding FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      IsEditable="True"
                      IsTextSearchEnabled="False"
                      Height="28"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,0,0,8">
            <TextBlock Text="Quantity" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox Text="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="28"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="CANCEL"
                    Width="96"
                    Height="30"
                    Margin="0,0,8,0"
                    Command="{Binding CancelCommand}"/>
            <Button Content="SAVE"
                    Width="96"
                    Height="30"
                    Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Window>
