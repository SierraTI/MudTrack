<UserControl x:Class="ProjectReport.Views.Geometry.DrillString.DrillStringGeometryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:ProjectReport.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="1200">

    <UserControl.Resources>
        <!-- Converters -->
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:CollectionToVisibilityConverter x:Key="CollectionToVis"/>

        <!-- Brushes -->
        <SolidColorBrush x:Key="Surface" Color="White"/>
        <SolidColorBrush x:Key="BorderSoft" Color="#E6E8EB"/>
        <SolidColorBrush x:Key="TextMuted" Color="#6B7280"/>
        <SolidColorBrush x:Key="TextStrong" Color="#111827"/>
        <SolidColorBrush x:Key="BrandOrange" Color="#F57C1F"/>
        <SolidColorBrush x:Key="BrandOrangeHover" Color="#EF6C00"/>
        <SolidColorBrush x:Key="Danger" Color="#DC2626"/>

        <Style x:Key="CardBorder" TargetType="Border" xmlns:sys="clr-namespace:System">
            <Setter Property="Background" Value="{StaticResource Surface}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14"/>
        </Style>

        <!-- Button Styles -->
        <Style x:Key="BrandPrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BrandOrange}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- DataGrid Style -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource Surface}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FFF9FAFB"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="RowHeight" Value="36"/>
            <Setter Property="ColumnHeaderHeight" Value="38"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with Totals and Add Button -->
        <Border Grid.Row="0" Style="{StaticResource CardBorder}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Total Length:"
                               Foreground="{StaticResource TextMuted}"
                               FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalDrillStringLength}"
                               Foreground="{StaticResource BrandOrange}"
                               FontWeight="Bold"/>
                    <TextBlock Text="  ft  |  Total Volume:"
                               Foreground="{StaticResource TextMuted}"
                               FontWeight="SemiBold"
                               Margin="20,0,0,0"/>
                    <TextBlock Text="{Binding TotalDrillStringVolume}"
                               Foreground="{StaticResource BrandOrange}"
                               FontWeight="Bold"/>
                    <TextBlock Text="  bbl"
                               Foreground="{StaticResource TextMuted}"
                               FontWeight="SemiBold"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="Add Component"
                        Style="{StaticResource BrandPrimaryButton}"
                        Command="{Binding AddDrillStringComponentCommand}"/>
            </Grid>
        </Border>

        <!-- Drill String Components DataGrid -->
        <DataGrid Grid.Row="1"
                  Style="{StaticResource ModernDataGrid}"
                  ItemsSource="{Binding DrillStringComponents}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single">

            <DataGrid.Columns>
                <!-- ID Column -->
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" IsReadOnly="True"/>

                <!-- Component Type Column -->
                <DataGridTemplateColumn Header="Type" Width="140">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ComponentType}" VerticalAlignment="Center" Padding="10,6"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding DataContext.DrillStringComponentTypes, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                      SelectedItem="{Binding ComponentType, UpdateSourceTrigger=PropertyChanged}"
                                      DisplayMemberPath="."/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <!-- Length Column -->
                <DataGridTextColumn Header="Length (ft)" 
                                    Binding="{Binding Length, StringFormat=F2, TargetNullValue=''}" 
                                    Width="120"/>

                <!-- OD Column -->
                <DataGridTextColumn Header="OD (in)" 
                                    Binding="{Binding OD, StringFormat=F3, TargetNullValue=''}" 
                                    Width="110"/>

                <!-- ID Column -->
                <DataGridTextColumn Header="ID (in)" 
                                    Binding="{Binding ID, StringFormat=F3, TargetNullValue=''}" 
                                    Width="110"/>

                <!-- Internal Volume Column -->
                <DataGridTextColumn Header="Internal Vol (bbl)" 
                                    Binding="{Binding InternalVolume, StringFormat=F2, TargetNullValue=''}" 
                                    Width="140" 
                                    IsReadOnly="True"/>

                <!-- Displacement Volume Column -->
                <DataGridTextColumn Header="Displacement (bbl)" 
                                    Binding="{Binding DisplacementVolume, StringFormat=F2, TargetNullValue=''}" 
                                    Width="140" 
                                    IsReadOnly="True"/>

                <!-- Actions Column -->
                <DataGridTemplateColumn Header="Actions" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="↑" Padding="8,4" MinWidth="30" ToolTip="Move Up" Margin="2,0"/>
                                <Button Content="↓" Padding="8,4" MinWidth="30" ToolTip="Move Down" Margin="2,0"/>
                                <Button Content="×" Padding="8,4" MinWidth="30" ToolTip="Delete" Foreground="{StaticResource Danger}" Margin="2,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
