<UserControl x:Class="ProjectReport.Views.Inventory.InventoryHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Movements}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Date"
                                    Binding="{Binding Date, StringFormat={}{0:yyyy-MM-dd HH:mm}}"
                                    Width="*" />

                <DataGridTextColumn Header="Product"
                                    Binding="{Binding ProductName}"
                                    Width="2*" />

                <DataGridTextColumn Header="Type"
                                    Binding="{Binding Type}"
                                    Width="*" />

                <DataGridTextColumn Header="Qty"
                                    Binding="{Binding Quantity}"
                                    Width="*" />

                <DataGridTextColumn Header="Unit Price"
                                    Binding="{Binding UnitPrice, StringFormat={}{0:0.##}}"
                                    Width="*" />

                <DataGridTextColumn Header="Origin / Use"
                                    Binding="{Binding OriginOrUse}"
                                    Width="2*" />

                <DataGridTextColumn Header="User"
                                    Binding="{Binding User}"
                                    Width="*" />

                <DataGridTextColumn Header="Stock Before"
                                    Binding="{Binding StockBefore}"
                                    Width="*" />

                <DataGridTextColumn Header="Stock After"
                                    Binding="{Binding StockAfter}"
                                    Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
