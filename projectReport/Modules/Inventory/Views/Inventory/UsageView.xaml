<UserControl x:Class="ProjectReport.Views.Inventory.UsageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ProjectReport.Views.Inventory"
             mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </UserControl.Resources>
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Daily Balance Usage Table -->
        <DataGrid Grid.Row="0"
                  x:Name="UsageDataGrid"
                  ItemsSource="{Binding BalanceItems}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionUnit="CellOrRowHeader"
                  SelectionMode="Single"
                  Margin="0,0,0,10"
                  Background="White"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1"
                  BorderBrush="#E5E7EB">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding ProductName}" Width="2*"/>
                <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="*"/>
                <DataGridTextColumn Header="Initial" Binding="{Binding InitialQuantity, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="Received" Binding="{Binding ReceivedQuantity, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="Return" Binding="{Binding ReturnQuantity, StringFormat={}{0:N2}}" Width="*"/>

                <!-- Use Detail Button -->
                <DataGridTemplateColumn Header="Use" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="USE" 
                                    Command="{Binding DataContext.OpenUseSpecificationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Background="#3B82F6" Foreground="White"
                                    FontWeight="Bold" FontSize="10"
                                    Padding="8,2" Height="24"
                                    BorderThickness="0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Total Used" Binding="{Binding TotalUsedQuantity, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="Current Stock" Binding="{Binding CurrentStock, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="Unit Cost" Binding="{Binding UnitCost, StringFormat={}{0:C2}}" Width="*"/>
                <DataGridTextColumn Header="Daily Cost" Binding="{Binding DailyCost, StringFormat={}{0:C2}}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="#059669"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Total Cost Footer -->
        <Border Grid.Row="1" Padding="12" BorderThickness="1" BorderBrush="#DDDDDD" CornerRadius="6" Margin="0,8,0,0">
            <DockPanel>
                <TextBlock Text="Daily Total Cost (Usage)" FontWeight="Bold" DockPanel.Dock="Left"/>
                <TextBlock FontWeight="Bold" DockPanel.Dock="Right" Foreground="#059669" 
                           Text="{Binding TotalProductsCost, StringFormat={}{0:C2}}"/>
            </DockPanel>
        </Border>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="SAVE" Width="110" Command="{Binding SaveCommand}"
                    Background="#10B981" Foreground="White" FontWeight="SemiBold"/>
        </StackPanel>

    </Grid>
</UserControl>
