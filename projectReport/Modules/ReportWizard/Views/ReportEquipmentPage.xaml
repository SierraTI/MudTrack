<UserControl x:Class="ProjectReport.Views.ReportWizard.ReportEquipmentPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#F7F8FA">

    <UserControl.Resources>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E6E8EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Rig Equipment Operations" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

                <!-- PUMPS SECTION -->
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Mud Pump Operations" Style="{StaticResource SectionTitle}"/>
                        <TextBlock Text="Enter SPM and Pressure for each active pump. GPM is auto-calculated based on Rig Profile Specs." 
                                   FontSize="12" Foreground="#6B7280" Margin="0,0,0,12"/>
                        
                        <DataGrid ItemsSource="{Binding Report.Pumps}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="#F3F4F6">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="No" Binding="{Binding No}" IsReadOnly="True" Width="40"/>
                                <DataGridTextColumn Header="Pump Name" Binding="{Binding PumpName}" IsReadOnly="True" Width="*"/>
                                <DataGridTextColumn Header="Liner (in)" Binding="{Binding LinerSize, StringFormat=N2}" IsReadOnly="True" Width="80"/>
                                <DataGridTextColumn Header="Stroke (in)" Binding="{Binding StrokeLength, StringFormat=N0}" IsReadOnly="True" Width="80"/>
                                <DataGridTextColumn Header="Eff (%)" Binding="{Binding Efficiency, StringFormat=N0}" IsReadOnly="True" Width="60"/>
                                
                                <DataGridTemplateColumn Header="SPM" Width="90">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Spm, UpdateSourceTrigger=PropertyChanged}" 
                                                     HorizontalContentAlignment="Right" Padding="5,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="GPM" Binding="{Binding Gpm, StringFormat=N1}" IsReadOnly="True" Width="90">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTemplateColumn Header="Press (psi)" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Pressure, UpdateSourceTrigger=PropertyChanged}" 
                                                     HorizontalContentAlignment="Right" Padding="5,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- SOLIDS CONTROL / SCREENS SECTION -->
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Solids Control Screens" Style="{StaticResource SectionTitle}"/>
                        <TextBlock Text="Enter the number of screens replaced for each shaker. This will deduct stock from Inventory." 
                                   FontSize="12" Foreground="#6B7280" Margin="0,0,0,12"/>

                        <DataGrid ItemsSource="{Binding Report.Screens}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="#F3F4F6">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Equipment" Binding="{Binding ShakerName}" IsReadOnly="True" Width="*"/>
                                <DataGridTextColumn Header="Screen Type" Binding="{Binding ScreenType}" IsReadOnly="True" Width="200"/>
                                
                                <DataGridTemplateColumn Header="Qty Used" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" 
                                                     HorizontalContentAlignment="Center" Padding="5,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridCheckBoxColumn Header="Deducted" Binding="{Binding IsDeducted}" IsReadOnly="True" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
                
                <!-- HYDRAULICS SUMMARY -->
                <Border Style="{StaticResource Card}" Background="#EEF2FF" BorderBrush="#C7D2FE">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Cumulative Flow Rate:" VerticalAlignment="Center" Foreground="#373185" FontWeight="SemiBold" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding Report.TotalGpm, StringFormat={}{0:N1} gpm}" FontSize="20" FontWeight="Bold" Foreground="#4338CA"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <TextBlock Text="Estimated Surface Pressure Loss:" VerticalAlignment="Center" Foreground="#373185" FontWeight="SemiBold" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding Report.SurfacePressureLoss, StringFormat={}{0:N0} psi}" FontSize="20" FontWeight="Bold" Foreground="#4338CA"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
