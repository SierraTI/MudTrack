<UserControl x:Class="ProjectReport.Modules.VolumeBalance.Views.AdditionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:add="clr-namespace:ProjectReport.Modules.VolumeBalance.Views.Additions">

    <!-- ===================== -->
    <!-- RESOURCES GLOBALES -->
    <!-- ===================== -->
    <UserControl.Resources>

        <!-- PALETA -->
        <Color x:Key="PrimaryOrange">#EE7D32</Color>

        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryOrange}"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#F5F6F8"/>

        <!-- BOTONES -->
        <Style x:Key="BaseButton" TargetType="Button">
            <Setter Property="Background" Value="#EE7D32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="25"/>
        </Style>

        <Style x:Key="AddButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#EE7D32"/>
            <Setter Property="MinWidth" Value="90"/>
        </Style>

        <Style x:Key="DeleteButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="MinWidth" Value="90"/>
        </Style>

        <!-- CARD -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,18"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

    </UserControl.Resources>

    <!-- ===================== -->
    <!-- MAIN -->
    <!-- ===================== -->
    <Grid Background="{StaticResource BackgroundBrush}">

        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      CanContentScroll="True"
                      PanningMode="VerticalOnly">

            <Grid Margin="24">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- ===================== -->
                <!-- CARD 1 -->
                <!-- ===================== -->
                <add:AdditionsLiquidVolumeView Grid.Row="0"/>

                <!-- ===================== -->
                <!-- CARD 2 -->
                <!-- ===================== -->
                <add:AdditionsFluidVolumeView Grid.Row="1"/>

                <!-- ===================== -->
                <!-- CARD 3 -->
                <!-- ===================== -->
                <add:AdditionsChemicalVolumeView Grid.Row="2"/>

                <!-- ===================== -->
                <!-- CARD 4 -->
                <!-- ===================== -->
                <add:TransfersView Grid.Row="3"/>

            </Grid>

        </ScrollViewer>

    </Grid>

</UserControl>