<UserControl x:Class="ProjectReport.Modules.VolumeBalance.Views.ConcentrationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <UserControl.Resources>

        <!-- FONDO -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="#F3F4F6"/>

        <!-- CARD -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Margin" Value="20"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- DATAGRID -->
        <Style TargetType="DataGrid">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="RowHeight" Value="34"/>
            <Setter Property="ColumnHeaderHeight" Value="38"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#EE7D32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">

        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      CanContentScroll="True">

            <StackPanel>

                <!-- ===================== -->
                <!-- TÍTULO (CONTENEDOR SEPARADO) -->
                <!-- ===================== -->
                <Border Background="White"
                        CornerRadius="12"
                        Margin="20,20,20,10"
                        Padding="14"
                        BorderBrush="#E5E7EB"
                        BorderThickness="1">

                    <TextBlock Text="CONCENTRATIONS (ppb)"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="#111827"/>

                </Border>

                <!-- ===================== -->
                <!-- TABLA -->
                <!-- ===================== -->
                <Border Style="{StaticResource CardStyle}"
                        Margin="20,0,20,20">

                    <DataGrid AutoGenerateColumns="False"
                              Height="360">

                        <DataGrid.Columns>

                            <DataGridTextColumn Header="COD" Width="*"/>
                            <DataGridTextColumn Header="Chemical" Width="*"/>
                            <DataGridTextColumn Header="Conc activo" Width="*"/>
                            <DataGridTextColumn Header="conc reserva (sub type)" Width="*"/>
                            <DataGridTextColumn Header="conc reserva (sub type)" Width="*"/>
                            <DataGridTextColumn Header="conc reserva (sub type)" Width="*"/>
                            <DataGridTextColumn Header="conc reserva (sub type)" Width="*"/>
                            <DataGridTextColumn Header="conc Other (sub type)" Width="*"/>
                            <DataGridTextColumn Header="conc Other (sub type)" Width="2*"/>

                        </DataGrid.Columns>

                    </DataGrid>

                </Border>

            </StackPanel>

        </ScrollViewer>

    </Grid>

</UserControl>