<UserControl x:Class="ProjectReport.Modules.VolumeBalance.Views.VolSystemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <UserControl.Resources>

        <!-- FONDO -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="#F3F4F6"/>

        <!-- CARD -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Margin" Value="20,10,20,10"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- DATAGRID -->
        <Style TargetType="DataGrid">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="RowHeight" Value="34"/>
            <Setter Property="ColumnHeaderHeight" Value="38"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#EE7D32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">

        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      CanContentScroll="True">

            <StackPanel>

                <!-- ===================== -->
                <!-- TITULO GENERAL -->
                <!-- ===================== -->
                <Border Background="White"
                        CornerRadius="12"
                        Margin="20,20,20,10"
                        Padding="14"
                        BorderBrush="#E5E7EB"
                        BorderThickness="1">

                    <TextBlock Text="VOLUME SYSTEM"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="#111827"/>

                </Border>

                <!-- ===================== -->
                <!-- PITS VOLUME (7 COLUMNAS) -->
                <!-- ===================== -->
                <Border Style="{StaticResource CardStyle}">

                    <StackPanel>

                        <TextBlock Text="PITS VOLUME (bbl)"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,8"
                                   Foreground="#111827"/>

                        <DataGrid AutoGenerateColumns="False"
                                  Height="220">

                            <DataGrid.Columns>

                                <DataGridTextColumn Header="Pit System" Width="*"/>
                                <DataGridTextColumn Header="Pit Name" Width="*"/>
                                <DataGridTextColumn Header="Fluid Type" Width="*"/>
                                <DataGridTextColumn Header="Fluid Subtype" Width="*"/>
                                <DataGridTextColumn Header="Previous Volume" Width="*"/>
                                <DataGridTextColumn Header="Current Volume" Width="*"/>
                                <DataGridTextColumn Header="Desnsity" Width="*"/>

                            </DataGrid.Columns>

                        </DataGrid>

                    </StackPanel>

                </Border>

                <!-- ===================== -->
                <!-- HOLE VOLUME (2 COLUMNAS) -->
                <!-- ===================== -->
                <Border Style="{StaticResource CardStyle}"
        Padding="14"
        Margin="20,10,20,10">

                    <StackPanel>

                        <!-- TITULO -->
                        <TextBlock Text="HOLE VOLUME (bbl)"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"
                   Foreground="#111827"/>

                        <!-- 1 FILA - 2 COLUMNAS -->
                        <Grid>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- LABEL -->
                            <TextBlock Text="Hole Volume"
                       VerticalAlignment="Center"
                       FontSize="13"
                       Foreground="#6B7280"/>

                            <!-- VALUE -->
                            <Border Grid.Column="1"
                    Background="#F9FAFB"
                    CornerRadius="8"
                    Padding="8"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1">

                                <TextBlock Text="0.00"
                           FontSize="14"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>

                            </Border>

                        </Grid>

                    </StackPanel>

                </Border>

            </StackPanel>

        </ScrollViewer>

    </Grid>

</UserControl>