<UserControl x:Class="ProjectReport.Views.GeometryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:geometry="clr-namespace:ProjectReport.Views.Geometry"
             xmlns:converters="clr-namespace:ProjectReport.Converters"
             xmlns:wb="clr-namespace:ProjectReport.Models.Geometry.Wellbore"
             xmlns:ds="clr-namespace:ProjectReport.Models.Geometry.DrillString"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1200"
             Loaded="GeometryView_Loaded"
             KeyDown="GeometryView_KeyDown">

    <UserControl.Resources>
        <!-- Converters -->
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:CollectionToVisibilityConverter x:Key="CollectionToVis"/>
        <converters:NullToVisibilityConverter x:Key="NullToVis"/>
        <converters:BoolToGreenRedConverter x:Key="BoolToGreenRedConverter"/>
        <converters:BitStatusConverter x:Key="BitStatusConverter"/>
        <converters:InvertedValueConverter x:Key="InvertedValueConverter"/>

        <CollectionViewSource x:Key="AnnularDetailsView" Source="{Binding AnnularVolumeDetails}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Stage"/>
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>


        <!-- Component specific gradients -->
        <LinearGradientBrush x:Key="BrandBlueGradient" StartPoint="0,0.5" EndPoint="1,0.5">
            <GradientStop Color="#0062FF" Offset="0"/>
            <GradientStop Color="#00C2FF" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BrandOrangeGradient" StartPoint="0,0.5" EndPoint="1,0.5">
            <GradientStop Color="#F57C1F" Offset="0"/>
            <GradientStop Color="#FFB74D" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource Surface}"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style x:Key="CardHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextStrong}"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <!-- Buttons -->
        <!-- Buttons (solo UI, sin tocar lógica) -->
        <Style x:Key="BrandPrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BrandOrange}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrandOrange}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="MinWidth" Value="130"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10"
                                                ShadowDepth="2"
                                                Opacity="0.18"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BrandOrangeHover}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BrandOrangeHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BrandOrangePressed}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BrandOrangePressed}"/>
                                <Setter TargetName="Bd" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.99" ScaleY="0.99"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="Bd" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ModernDataGrid (Added to fix missing resource crash) -->


        <Style x:Key="BrandSecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#FFF3F4F6"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}"/>
            <Setter Property="Foreground" Value="{StaticResource TextStrong}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FFEFF2F7"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#FFD8DDE3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FFE5E7EB"/>
                                <Setter TargetName="Bd" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.99" ScaleY="0.99"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="Bd" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource BrandSecondaryButton}">
            <Setter Property="Foreground" Value="{StaticResource Danger}"/>
            <Setter Property="BorderBrush" Value="#FFF1B5B5"/>
        </Style>

        <Style x:Key="IconMiniButton" TargetType="Button" BasedOn="{StaticResource BrandSecondaryButton}">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="MinWidth" Value="36"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>


        <!-- DataGrid baseline -->
        <!-- Component specific styles if any -->

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FFF3F4F6"/>
            <Setter Property="Foreground" Value="{StaticResource TextStrong}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="6,2"/>
        </Style>

        <!-- GroupBox -->
        <Style TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{StaticResource Surface}"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.65"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Padding" Value="14,8"/>
        </Style>

    </UserControl.Resources>

    <Grid Background="{StaticResource AppBg}">
        <TabControl SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}">
            <!-- ========================= -->
            <!-- Wellbore Geometry -->
            <!-- ========================= -->
            <TabItem Header="Wellbore Geometry">

                <!-- Main Grid -->
                <Grid Margin="18">

                    <!-- Layout Rows -->
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- Top bar -->
                        <RowDefinition Height="Auto"/>
                        <!-- Error -->
                        <RowDefinition Height="*"/>
                        <!-- DataGrid -->
                    </Grid.RowDefinitions>


                    <!-- ================= TOP BAR ================= -->
                    <Border Grid.Row="0"
               Style="{StaticResource CardBorder}"
               Margin="0,0,0,12">

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0"
                           Orientation="Horizontal"
                           VerticalAlignment="Center">

                                <TextBlock Text="Total Wellbore MD:"
                              Foreground="{StaticResource TextMuted}"
                              FontWeight="SemiBold"/>

                                <TextBlock Text="{Binding TotalWellboreMD,
                              StringFormat='  {0:F2} ft'}"
                              Foreground="{StaticResource BrandOrange}"
                              FontWeight="Bold"/>

                                <TextBlock Text="   Tip: use '.' for decimals."
                              Foreground="{StaticResource TextMuted}"
                              FontSize="12"
                              VerticalAlignment="Center"/>
                            </StackPanel>

                            <Button Grid.Column="1"
                       Content="Add Section"
                       Style="{StaticResource BrandPrimaryButton}"
                       Click="AddWellboreSection_Click"/>

                        </Grid>
                    </Border>


                    <!-- ================= ERROR PANEL ================= -->
                    <Border Grid.Row="1"
                      Margin="0,0,0,10"
                      Background="#FFFEE2E2"
                      BorderBrush="{StaticResource Danger}"
                      BorderThickness="1"
                      CornerRadius="10"
                      Padding="12"
                      Visibility="{Binding ElementName=WellboreDataGrid, Path=SelectedItem.HasValidationError, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="❌" FontSize="14" Margin="0,0,8,0" Foreground="{StaticResource Danger}"/>
                            <TextBlock Text="Error in selected row:" FontWeight="Bold" Foreground="{StaticResource Danger}" Margin="0,0,6,0"/>
                            <TextBlock Text="{Binding ElementName=WellboreDataGrid, Path=SelectedItem.ValidationMessage}"
                                 Foreground="{StaticResource Danger}"
                                 TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>


                    <!-- ================= DATAGRID ================= -->
                    <DataGrid Grid.Row="2"
                 x:Name="WellboreDataGrid"
                 Style="{StaticResource ModernDataGrid}"
                 ItemsSource="{Binding WellboreComponents}"
                 AutoGenerateColumns="False"
                 CanUserAddRows="False"
                 CanUserDeleteRows="True"
                 SelectionMode="Single"
                 AlternationCount="2"

                 Margin="0"

                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto">


                        <!-- ================= ROW STYLE ================= -->
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">

                                <Setter Property="Background" Value="White"/>

                                <Style.Triggers>

                                    <Trigger Property="AlternationIndex" Value="1">
                                        <Setter Property="Background" Value="#FFF9FAFB"/>
                                    </Trigger>

                                    <DataTrigger Binding="{Binding HasWarnings}" Value="True">
                                        <Setter Property="Background" Value="#FFFFF7E6"/>
                                        <Setter Property="BorderBrush" Value="#FFF59E0B"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                    </DataTrigger>

                                    <DataTrigger Binding="{Binding HasValidationError}" Value="True">
                                        <Setter Property="Background" Value="#FFFFF1F2"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource Danger}"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                    </DataTrigger>

                                </Style.Triggers>

                            </Style>
                        </DataGrid.RowStyle>


                        <!-- ================= COLUMNS ================= -->
                        <DataGrid.Columns>

                            <!-- Status -->
                            <DataGridTemplateColumn Header="" Width="30">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>

                                            <TextBlock Text="⚠️"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          ToolTip="{Binding WarningMessage}"
                                          Visibility="{Binding HasWarnings,
                                          Converter={StaticResource BoolToVis}}"/>

                                            <TextBlock Text="!"
                                          Foreground="{StaticResource Danger}"
                                          FontWeight="Bold"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          ToolTip="{Binding ValidationMessage}"
                                          Visibility="{Binding HasValidationError,
                                          Converter={StaticResource BoolToVis}}"/>

                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- No. Column -->
                            <DataGridTextColumn Header="No." Binding="{Binding Id}" Width="35" IsReadOnly="True"
                                                   Foreground="{StaticResource TextMuted}"/>

                            <!-- Name / Description Column -->
                            <DataGridTemplateColumn Header="Section Name" Width="130">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <TextBlock Text="Ej: Intermediate Casing"
                                                          Foreground="{StaticResource DisabledText}"
                                                          FontStyle="Italic"
                                                          VerticalAlignment="Center"
                                                          Margin="10,0,0,0"
                                                          IsHitTestVisible="False">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Name}" Value="">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Name}" Value="{x:Null}">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Padding="10,6"/>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Padding="8,6"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <!-- Well Section Type Column (NEW) -->
                            <DataGridTemplateColumn Header="Well Section" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock VerticalAlignment="Center" Padding="10,6">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="{Binding WellSection}"/>
                                                    <Setter Property="Foreground" Value="{StaticResource TextStrong}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding WellSection}" Value="{x:Null}">
                                                            <Setter Property="Text" Value="-- Select --"/>
                                                            <Setter Property="Foreground" Value="{StaticResource DisabledText}"/>
                                                            <Setter Property="FontStyle" Value="Italic"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding DataContext.WellboreSectionTypes, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                     SelectedItem="{Binding WellSection, UpdateSourceTrigger=PropertyChanged}"
                                                     DisplayMemberPath="."/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <!-- Component Column (Renamed from "Section Type") -->
                            <DataGridTemplateColumn Header="Component" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock VerticalAlignment="Center" Padding="10,6">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="{Binding Component}"/>
                                                    <Setter Property="Foreground" Value="{StaticResource TextStrong}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Component}" Value="{x:Null}">
                                                            <Setter Property="Text" Value="-- Select --"/>
                                                            <Setter Property="Foreground" Value="{StaticResource DisabledText}"/>
                                                            <Setter Property="FontStyle" Value="Italic"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding DataContext.ComponentTypes, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                     SelectedItem="{Binding Component, UpdateSourceTrigger=PropertyChanged}"
                                                     DisplayMemberPath="."/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <!-- 3. OD -->
                            <DataGridTemplateColumn Width="85">
                                <DataGridTemplateColumn.Header>
                                    <TextBlock Text="OD (in)" ToolTip="For OpenHole: Hole diameter. For Casing/Liner: Outer diameter."/>
                                </DataGridTemplateColumn.Header>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding OD, TargetNullValue=''}" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasWarnings}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource Warn}"/>
                                                            <Setter Property="ToolTip" Value="{Binding WarningMessage}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding HasErrors}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource Danger}"/>
                                                            <Setter Property="ToolTip" Value="{Binding ValidationMessage}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding OD, TargetNullValue='', UpdateSourceTrigger=LostFocus}">
                                            <TextBox.Style>
                                                <Style TargetType="TextBox">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasErrors}" Value="True">
                                                            <Setter Property="Background" Value="#FFFFF1F2"/>
                                                            <Setter Property="BorderBrush" Value="{StaticResource Danger}"/>
                                                            <Setter Property="ToolTip" Value="{Binding ValidationMessage}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBox.Style>
                                        </TextBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <!-- 4. ID -->
                            <DataGridTemplateColumn Width="85">
                                <DataGridTemplateColumn.Header>
                                    <TextBlock Text="ID (in)" ToolTip="For OpenHole: always 0.000. For Casing/Liner: Inner diameter."/>
                                </DataGridTemplateColumn.Header>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ID, TargetNullValue=''}" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Component}" Value="{x:Static ds:ComponentType.OpenHole}">
                                                            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                            <Setter Property="FontStyle" Value="Italic"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding HasErrors}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource Danger}"/>
                                                            <Setter Property="ToolTip" Value="{Binding ValidationMessage}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding ID, TargetNullValue='',  UpdateSourceTrigger=LostFocus}">
                                            <TextBox.Style>
                                                <Style TargetType="TextBox">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Component}" Value="{x:Static ds:ComponentType.OpenHole}">
                                                            <Setter Property="IsEnabled" Value="False"/>
                                                            <Setter Property="Background" Value="#FFF3F4F6"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding HasErrors}" Value="True">
                                                            <Setter Property="Background" Value="#FFFFF1F2"/>
                                                            <Setter Property="BorderBrush" Value="{StaticResource Danger}"/>
                                                            <Setter Property="ToolTip" Value="{Binding ValidationMessage}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBox.Style>
                                        </TextBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <!-- 5. Top MD -->
                            <DataGridTextColumn Header="Top MD (ft)" Binding="{Binding TopMD, StringFormat=0.##, TargetNullValue=''}" Width="100"/>

                            <!-- 6. Bottom MD -->
                            <DataGridTextColumn Header="Bottom MD (ft)" Binding="{Binding BottomMD, StringFormat=0.##, TargetNullValue=''}" Width="110"/>


                            <!-- 7. Washout -->
                            <DataGridTemplateColumn Width="95">
                                <DataGridTemplateColumn.Header>
                                    <TextBlock Text="Washout (%)" ToolTip="Only for OpenHole. Typical 5–25%. Min 0.01%."/>
                                </DataGridTemplateColumn.Header>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="N/A"/>
                                                    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                    <Setter Property="FontStyle" Value="Italic"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Component}" Value="{x:Static ds:ComponentType.OpenHole}">
                                                            <Setter Property="Text" Value="{Binding Washout, StringFormat=0.##}"/>
                                                            <Setter Property="Foreground" Value="{StaticResource TextStrong}"/>
                                                            <Setter Property="FontStyle" Value="Normal"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding HasWarnings}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource Warn}"/>
                                                            <Setter Property="ToolTip" Value="{Binding WarningMessage}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding HasErrors}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource Danger}"/>
                                                            <Setter Property="ToolTip" Value="{Binding ValidationMessage}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Washout, StringFormat=0.##, TargetNullValue='', UpdateSourceTrigger=PropertyChanged}">
                                            <TextBox.Style>
                                                <Style TargetType="TextBox">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Component}" Value="{x:Static ds:ComponentType.OpenHole}">
                                                            <Setter Property="IsEnabled" Value="True"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding HasErrors}" Value="True">
                                                            <Setter Property="Background" Value="#FFFFF1F2"/>
                                                            <Setter Property="BorderBrush" Value="{StaticResource Danger}"/>
                                                            <Setter Property="ToolTip" Value="{Binding ValidationMessage}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBox.Style>
                                        </TextBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <!-- 8. Volume -->
                            <DataGridTextColumn Header="Volume (bbl)" Binding="{Binding Volume, StringFormat=0.##}" Width="100" IsReadOnly="True"/>

                            <!-- 9. Actions -->
                            <DataGridTemplateColumn Header="Actions" Width="140">

                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>

                                        <StackPanel Orientation="Horizontal"
                       HorizontalAlignment="Center">

                                            <Button Content="↑"
                       Style="{StaticResource IconMiniButton}"
                       ToolTip="Move Up"
                       Click="MoveWellboreUp_Click"
                       Tag="{Binding}"/>

                                            <Button Content="↓"
                       Style="{StaticResource IconMiniButton}"
                       ToolTip="Move Down"
                       Click="MoveWellboreDown_Click"
                       Tag="{Binding}"/>

                                            <Button Content="×"
                       Style="{StaticResource IconMiniButton}"
                       ToolTip="Delete"
                       Click="DeleteWellbore_Click"
                       Tag="{Binding}"
                       Foreground="{StaticResource Danger}"/>

                                        </StackPanel>

                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>

                            </DataGridTemplateColumn>

                        </DataGrid.Columns>

                    </DataGrid>

                </Grid>


            </TabItem>
            
            <!-- ========================= -->
            <!-- Drill String Geometry -->
            <!-- ========================= -->
            <TabItem Header="Drill String Geometry">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="18">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Drill String Top barra -->
                        <Border Grid.Row="0" Style="{StaticResource CardBorder}" Margin="0,0,0,12">
                            <Grid VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- MÉTRICAS -->
                                <WrapPanel Grid.Column="0"
                                        VerticalAlignment="Center"
                                        ItemHeight="26">

                                    <TextBlock Text="Total Drill String MD:"
                                            Foreground="{StaticResource TextMuted}"
                                            FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding TotalDrillStringLength, StringFormat='  {0:F2} ft'}"
                                            Foreground="{StaticResource BrandOrange}"
                                            FontWeight="Bold"
                                            Margin="4,0,16,0"/>

                                    <TextBlock Text="Bottom Status:"
                                            Foreground="{StaticResource TextMuted}"
                                            FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding DepthDifferentialStatus}"
                                            FontWeight="Bold"
                                            Foreground="{Binding DepthDifferentialColor}"
                                            Margin="4,0,16,0"/>

                                    <TextBlock Text="Total Wellbore MD:"
                                            Foreground="{StaticResource TextMuted}"
                                            FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding TotalWellboreMD, StringFormat='  {0:F2} ft'}"
                                            Foreground="{StaticResource BrandOrange}"
                                            FontWeight="Bold"/>
                                </WrapPanel>

                                <!-- ACCIONES -->
                                <StackPanel Grid.Column="1"
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center">

                                    <Button Content="Adjust to Bottom"
                                            Style="{StaticResource BrandPrimaryButton}"
                                            Command="{Binding ForceToBottomCommand}"
                                            IsEnabled="{Binding CanForceToBottom}"
                                            Margin="0,0,10,0"/>

                                    <Button Content="Add Component"
                                            Style="{StaticResource BrandPrimaryButton}"
                                            MinWidth="140"
                                            Command="{Binding AddDrillStringComponentCommand}"/>
                                </StackPanel>
                            </Grid>
                        </Border>


                        <!-- Área de Trabajo Principal (Tabla + Visualizador) -->
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <DockPanel Grid.Column="0">
                                <!-- Error detail -->
                                <Border DockPanel.Dock="Top"
                                        Margin="0,0,0,10"
                                        Background="#FFFEE2E2"
                                        BorderBrush="{StaticResource Danger}"
                                        BorderThickness="1"
                                        CornerRadius="10"
                                        Padding="12"
                                        Visibility="{Binding ElementName=DrillStringDataGrid, Path=SelectedItem.HasValidationError, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="❌" FontSize="14" Margin="0,0,8,0" Foreground="{StaticResource Danger}"/>
                                        <TextBlock Text="Error in selected row:" FontWeight="Bold" Foreground="{StaticResource Danger}" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding ElementName=DrillStringDataGrid, Path=SelectedItem.ValidationMessage}"
                                                   Foreground="{StaticResource Danger}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>

                                <!-- DataGrid with Zebra Striping -->
                                <DataGrid x:Name="DrillStringDataGrid"
                                          Style="{StaticResource ModernDataGrid}"
                                          ItemsSource="{Binding DrillStringComponents}"
                                          AutoGenerateColumns="False"
                                          CanUserAddRows="False"
                                          CanUserDeleteRows="True"
                                          SelectionMode="Single"
                                          CanUserReorderColumns="False"
                                          AllowDrop="True"
                                          Drop="DrillStringDataGrid_Drop"
                                          PreviewMouseLeftButtonDown="DrillStringDataGrid_PreviewMouseLeftButtonDown"
                                          PreviewMouseMove="DrillStringDataGrid_PreviewMouseMove"
                                          AlternationCount="2">

                                    <DataGrid.RowStyle>
                                        <Style TargetType="DataGridRow">
                                            <!-- Zebra Striping: Alternating row colors -->
                                            <Setter Property="Background" Value="White"/>
                                            <Style.Triggers>
                                                <Trigger Property="AlternationIndex" Value="1">
                                                    <Setter Property="Background" Value="#FFF9FAFB"/>
                                                </Trigger>
                                                <DataTrigger Binding="{Binding HasValidationError}" Value="True">
                                                    <Setter Property="Background" Value="#FFFFF1F2"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource Danger}"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                                                    <Setter Property="Background" Value="#FFEAFBF0"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource Success}"/>
                                                    <Setter Property="BorderThickness" Value="2"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGrid.RowStyle>

                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Header="" Width="30">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="❌"
                                                       Foreground="{StaticResource Danger}"
                                                       FontWeight="Bold"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       ToolTip="{Binding ValidationMessage}"
                                                       Visibility="{Binding HasValidationError, Converter={StaticResource BoolToVis}}"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <DataGridTextColumn Header="No." Binding="{Binding Id}" Width="40" IsReadOnly="True"
                                                    Foreground="{StaticResource TextMuted}"/>

                                        <!-- Component Name with Placeholder -->
                                        <DataGridTemplateColumn Header="Component Name" Width="140">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="🔗" Margin="5,0,0,0" VerticalAlignment="Center"
                                                           ToolTip="Tool Joint Configured" 
                                                           Visibility="{Binding IsToolJointConfigured, Converter={StaticResource BoolToVis}}"/>

                                                        <Grid Margin="5,0,0,0">
                                                            <TextBlock Text="Ej: DC"
                                                               Foreground="{StaticResource DisabledText}"
                                                               FontStyle="Italic"
                                                               VerticalAlignment="Center"
                                                               IsHitTestVisible="False">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding Name}" Value="">
                                                                                <Setter Property="Visibility" Value="Visible"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding Name}" Value="{x:Null}">
                                                                                <Setter Property="Visibility" Value="Visible"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Padding="0,6"/>
                                                        </Grid>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                            <DataGridTemplateColumn.CellEditingTemplate>
                                                <DataTemplate>
                                                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Padding="8,6"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellEditingTemplate>
                                        </DataGridTemplateColumn>

                                        <!-- Component Type Column (Placeholder added) -->
                                        <DataGridTemplateColumn Header="Type" Width="110">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock VerticalAlignment="Center" Padding="10,6">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Text" Value="{Binding ComponentTypeString}"/>
                                                                <Setter Property="Foreground" Value="{StaticResource TextStrong}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding ComponentTypeString}" Value="Drill Pipe">
                                                                        <!-- We only want placeholder if it's "New" or null logic -->
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Name}" Value="">
                                                                        <Setter Property="Text" Value="-- Select --"/>
                                                                        <Setter Property="Foreground" Value="{StaticResource DisabledText}"/>
                                                                        <Setter Property="FontStyle" Value="Italic"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Name}" Value="{x:Null}">
                                                                        <Setter Property="Text" Value="-- Select --"/>
                                                                        <Setter Property="Foreground" Value="{StaticResource DisabledText}"/>
                                                                        <Setter Property="FontStyle" Value="Italic"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                            <DataGridTemplateColumn.CellEditingTemplate>
                                                <DataTemplate>
                                                    <ComboBox ItemsSource="{Binding DataContext.DrillStringComponentTypes, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                      SelectedItem="{Binding ComponentTypeString, UpdateSourceTrigger=PropertyChanged}"
                                                      DisplayMemberPath="."/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellEditingTemplate>
                                        </DataGridTemplateColumn>

                                        <DataGridTemplateColumn Header="Properties" Width="70">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="⚙️" Style="{StaticResource IconMiniButton}" 
                                                    Visibility="{Binding IsConfigurable, Converter={StaticResource BoolToVis}}"
                                                    Click="ConfigureComponent_Click" Tag="{Binding}"
                                                    ToolTip="Configure Component Properties"
                                                    HorizontalAlignment="Center"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <DataGridTextColumn Header="Length (ft)" Binding="{Binding Length, StringFormat=0.##}" Width="100"/>
                                        <DataGridTextColumn Header="OD (in)" 
                    Binding="{Binding OD, UpdateSourceTrigger=LostFocus, ValidatesOnDataErrors=True}" 
                    Width="85"/>
                                        <DataGridTextColumn Header="ID (in)" 
                    Binding="{Binding ID, UpdateSourceTrigger=LostFocus}" 
                    Width="85"/>


                                        <DataGridTextColumn Header="Volume (bbl)" Binding="{Binding InternalVolume, StringFormat=0.##}" Width="100" IsReadOnly="True"/>

                                        <DataGridTemplateColumn Header="Actions" Width="140">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                       <Button Content="↑" Style="{StaticResource IconMiniButton}" ToolTip="Move Up"
                                                        Command="{Binding DataContext.MoveDrillStringUpCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                        CommandParameter="{Binding}"/>
                                                        <Button Content="↓" Style="{StaticResource IconMiniButton}" ToolTip="Move Down"
                                                        Command="{Binding DataContext.MoveDrillStringDownCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                        CommandParameter="{Binding}"/>
                                                        <Button Content="×" Style="{StaticResource IconMiniButton}" ToolTip="Delete"
                                                        Command="{Binding DataContext.DeleteDrillStringComponentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                        CommandParameter="{Binding}" Foreground="{StaticResource Danger}"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </DockPanel>

                            <!-- Visualizador Gráfico Proporcional -->
                            <Border Grid.Column="1" 
                            Width="300"
                            Margin="12,0,0,0"
                            Style="{StaticResource CardBorder}"
                            Background="White">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <Border Background="#FFF3F4F6" CornerRadius="8,8,0,0" Padding="10">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <TextBlock Text="BHA SCHEMATIC" FontWeight="Bold" Foreground="{StaticResource BrandBlue}"/>
                                            <Border Margin="10,0,0,0" 
                                            Padding="6,2" 
                                            CornerRadius="4"
                                            Background="{Binding IsOnBottom, Converter={StaticResource BoolToGreenRedConverter}}"
                                            Visibility="{Binding IsOnBottom, Converter={StaticResource BoolToVis}}">
                                                <TextBlock Text="ON BOTTOM" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                            </Border>
                                        </StackPanel>
                                    </Border>

                                    <geometry:BhaSchematicView x:Name="BhaVisualizer" 
                                                            Grid.Row="1"
                                                            Margin="10"/>

                                    <!-- No Data Overlay -->
                                    <TextBlock Grid.Row="1" 
                                       Text="No BHA components defined" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Foreground="{StaticResource TextMuted}"
                                       Visibility="{Binding DrillStringComponents.Count, Converter={StaticResource CollectionToVis}, ConverterParameter=Inverse}"/>
                                </Grid>
                            </Border>
                        </Grid>


                    </Grid>
                </ScrollViewer>
            </TabItem>

            <!-- ========================= -->
            <!-- Survey -->
            <!-- ========================= -->
            <TabItem Header="Survey">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="18">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto" MinHeight="250"/>
                            <!-- DataGrid Area -->
                            <RowDefinition Height="Auto"/>
                            <!-- Gap/Controls -->
                            <RowDefinition Height="Auto" MinHeight="350"/>
                            <!-- Chart Area -->
                            <RowDefinition Height="Auto"/>
                            <!-- Bottom Bar -->
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Style="{StaticResource CardBorder}" Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="Survey points define the trajectory. Keep MD increasing."
                                           Foreground="{StaticResource TextMuted}"
                                           VerticalAlignment="Center"/>

                                <Button Grid.Column="1"
                                        Content="Load from Excel"
                                        Style="{StaticResource BrandSecondaryButton}"
                                        Margin="0,0,10,0"
                                        Click="LoadSurveyFromExcel_Click"/>

                                <Button Grid.Column="2"
                                        Content="Add Survey Point"
                                        Style="{StaticResource BrandPrimaryButton}"
                                        Click="AddSurveyPoint_Click"/>
                            </Grid>
                        </Border>

                        <DataGrid Grid.Row="1"
                                  x:Name="SurveyDataGrid"
                                  Style="{StaticResource ModernDataGrid}"
                                  ItemsSource="{Binding SurveyPoints}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="True"
                                  SelectionMode="Single"
                                  MaxHeight="400">

                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasErrors}" Value="True">
                                            <Setter Property="Background" Value="#FFFFF1F2"/>
                                            <Setter Property="BorderBrush" Value="{StaticResource Danger}"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>

                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="" Width="34">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="!"
                                                       Foreground="{StaticResource Danger}"
                                                       FontWeight="Bold"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       ToolTip="{Binding ValidationMessage}"
                                                       Visibility="{Binding HasErrors, Converter={StaticResource BoolToVis}}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" IsReadOnly="True"/>

                                <DataGridTextColumn Header="MD (ft)" Binding="{Binding MD, StringFormat=0.##, TargetNullValue=''}" Width="130">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style TargetType="DataGridColumnHeader">
                                            <Setter Property="ToolTip" Value="Measured Depth along wellbore (INPUT)"/>
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                </DataGridTextColumn>

                                <!-- TVD - READ-ONLY CALCULATED -->
                                <DataGridTextColumn Header="TVD (ft)" Binding="{Binding TVD, StringFormat=0.##}" Width="130" IsReadOnly="True">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style TargetType="DataGridColumnHeader">
                                            <Setter Property="ToolTip" Value="True Vertical Depth (AUTO-CALCULATED)"/>
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Background" Value="#FFF3F4F6"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                            <Setter Property="FontStyle" Value="Italic"/>
                                            <Setter Property="Padding" Value="10,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Hole Angle (°)" Binding="{Binding HoleAngle, StringFormat=F4, TargetNullValue=''}" Width="160">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style TargetType="DataGridColumnHeader">
                                            <Setter Property="ToolTip" Value="Inclination from vertical, 0-93° (INPUT)"/>
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Azimuth (°)" Binding="{Binding Azimuth, StringFormat=F4, TargetNullValue=''}" Width="140">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style TargetType="DataGridColumnHeader">
                                            <Setter Property="ToolTip" Value="Direction from North, 0-360° (INPUT)"/>
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                </DataGridTextColumn>

                                <!-- Northing - READ-ONLY CALCULATED -->
                                <DataGridTextColumn Header="Northing (ft)" Binding="{Binding Northing, StringFormat=0.##}" Width="140" IsReadOnly="True">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style TargetType="DataGridColumnHeader">
                                            <Setter Property="ToolTip" Value="North coordinate (AUTO-CALCULATED)"/>
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Background" Value="#FFF3F4F6"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                            <Setter Property="FontStyle" Value="Italic"/>
                                            <Setter Property="Padding" Value="10,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Easting - READ-ONLY CALCULATED -->
                                <DataGridTextColumn Header="Easting (ft)" Binding="{Binding Easting, StringFormat=0.##}" Width="140" IsReadOnly="True">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style TargetType="DataGridColumnHeader">
                                            <Setter Property="ToolTip" Value="East coordinate (AUTO-CALCULATED)"/>
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Background" Value="#FFF3F4F6"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                            <Setter Property="FontStyle" Value="Italic"/>
                                            <Setter Property="Padding" Value="10,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Vertical Section - READ-ONLY CALCULATED -->
                                <DataGridTextColumn Header="Horiz. Disp. (ft)" Binding="{Binding VerticalSection, StringFormat=0.##}" Width="160" IsReadOnly="True">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style TargetType="DataGridColumnHeader">
                                            <Setter Property="ToolTip" Value="Horizontal Displacement from origin (AUTO-CALCULATED)"/>
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Background" Value="#FFF3F4F6"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                            <Setter Property="FontStyle" Value="Italic"/>
                                            <Setter Property="Padding" Value="10,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- DLS (Dogleg Severity) - READ-ONLY CALCULATED with color coding -->
                                <DataGridTextColumn Header="DLS (°/100ft)" Binding="{Binding DoglegSeverity, StringFormat=0.##}" Width="120" IsReadOnly="True">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style TargetType="DataGridColumnHeader">
                                            <Setter Property="ToolTip" Value="Dogleg Severity - Warning if &gt; 3°/100ft, Critical if &gt; 10°/100ft"/>
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Padding" Value="10,0"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsHighDogleg}" Value="True">
                                                    <Setter Property="Foreground" Value="#DC2626"/>
                                                    <Setter Property="Background" Value="#FEF2F2"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding DoglegSeverity}" Value="{x:Static sys:Double.NaN}">
                                                    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                    <Setter Property="Background" Value="#FFF3F4F6"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTemplateColumn Header="Actions" Width="160">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Content="↑" Style="{StaticResource IconMiniButton}" ToolTip="Move Up"
                                                        Click="MoveSurveyUp_Click" Tag="{Binding}"/>
                                                <Button Content="↓" Style="{StaticResource IconMiniButton}" ToolTip="Move Down"
                                                        Click="MoveSurveyDown_Click" Tag="{Binding}"/>
                                                <Button Content="×" Style="{StaticResource IconMiniButton}" ToolTip="Delete"
                                                        Click="DeleteSurvey_Click" Tag="{Binding}" Foreground="{StaticResource Danger}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <TextBlock Grid.Row="2" Text="Trajectory Visualization (Multi-View)" Style="{StaticResource CardHeader}" Margin="0,15,0,5"/>

                        <!-- Survey Charts - Multi-View -->
                        <Grid Grid.Row="3">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Row 1: Vertical Section (Profile View) and Plan View -->
                            <Grid Grid.Row="0" Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Vertical Section Chart (Profile View) -->
                                <Border Grid.Column="0" 
                                        Style="{StaticResource CardBorder}" 
                                        Padding="10" 
                                        ClipToBounds="True" 
                                        Height="350">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0"
                                                   Text="Profile View (Vertical Section vs TVD)" 
                                                   FontSize="12" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="{StaticResource TextMuted}"
                                                   Margin="0,0,0,5"
                                                   HorizontalAlignment="Left"/>

                                        <lvc:CartesianChart Grid.Row="1"
                                                            Series="{Binding SurveySeriesCollection}" 
                                                            LegendLocation="None" 
                                                            Zoom="None"
                                                            Hoverable="True">
                                            <lvc:CartesianChart.DataTooltip>
                                                <lvc:DefaultTooltip SelectionMode="SharedYValues" 
                                                                    ShowTitle="False"
                                                                    BulletSize="8"/>
                                            </lvc:CartesianChart.DataTooltip>
                                            <lvc:CartesianChart.AxisX>
                                                <lvc:Axis Title="Vertical Section (ft)" 
                                                          Foreground="{StaticResource TextStrong}"
                                                          MinValue="0"
                                                          MaxValue="{Binding MaxSurveyVerticalSection}">
                                                    <lvc:Axis.Separator>
                                                        <lvc:Separator Stroke="{StaticResource BorderSoft}" StrokeThickness="1"/>
                                                    </lvc:Axis.Separator>
                                                </lvc:Axis>
                                            </lvc:CartesianChart.AxisX>
                                            <lvc:CartesianChart.AxisY>
                                                <lvc:Axis Title="TVD (ft)" 
                                                          Foreground="{StaticResource TextStrong}" 
                                                          LabelFormatter="{Binding YAxisLabelFormatter}"
                                                          MinValue="{Binding MaxSurveyTVD, Converter={StaticResource InvertedValueConverter}}"
                                                          MaxValue="0">
                                                    <lvc:Axis.Separator>
                                                        <lvc:Separator Stroke="{StaticResource BorderSoft}" StrokeThickness="1" Step="500"/>
                                                    </lvc:Axis.Separator>
                                                    <lvc:Axis.Sections>
                                                        <lvc:SectionsCollection>
                                                            <lvc:AxisSection Value="{Binding ReportMD, Converter={StaticResource InvertedValueConverter}}" 
                                                                             Stroke="Red" 
                                                                             StrokeThickness="2" 
                                                                             StrokeDashArray="4,2"
                                                                             DataLabel="True"
                                                                             Label="Report MD"/>

                                                        </lvc:SectionsCollection>
                                                    </lvc:Axis.Sections>
                                                </lvc:Axis>
                                            </lvc:CartesianChart.AxisY>
                                        </lvc:CartesianChart>
                                    </Grid>
                                </Border>

                                <!-- Plan View Chart (North vs East) -->
                                <Border Grid.Column="2" 
                                        Style="{StaticResource CardBorder}" 
                                        Padding="10" 
                                        ClipToBounds="True" 
                                        Height="350">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0"
                                                   Text="Plan View (North vs East)" 
                                                   FontSize="12" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="{StaticResource TextMuted}"
                                                   Margin="0,0,0,5"
                                                   HorizontalAlignment="Left"/>

                                        <lvc:CartesianChart Grid.Row="1"
                                                            Series="{Binding PlanViewSeriesCollection}" 
                                                            LegendLocation="None" 
                                                            Zoom="None"
                                                            Hoverable="True">
                                            <lvc:CartesianChart.DataTooltip>
                                                <lvc:DefaultTooltip SelectionMode="SharedYValues" 
                                                                    ShowTitle="False"
                                                                    BulletSize="8"/>
                                            </lvc:CartesianChart.DataTooltip>
                                            <lvc:CartesianChart.AxisX>
                                                <lvc:Axis Title="North (ft)" 
                                                          Foreground="{StaticResource TextStrong}"
                                                          MinValue="{Binding MaxSurveyNorth, Converter={StaticResource InvertedValueConverter}}"
                                                          MaxValue="{Binding MaxSurveyNorth}">
                                                    <lvc:Axis.Separator>
                                                        <lvc:Separator Stroke="{StaticResource BorderSoft}" StrokeThickness="1"/>
                                                    </lvc:Axis.Separator>
                                                </lvc:Axis>
                                            </lvc:CartesianChart.AxisX>
                                            <lvc:CartesianChart.AxisY>
                                                <lvc:Axis Title="East (ft)" 
                                                          Foreground="{StaticResource TextStrong}"
                                                          MinValue="{Binding MaxSurveyEast, Converter={StaticResource InvertedValueConverter}}"
                                                          MaxValue="{Binding MaxSurveyEast}">
                                                    <lvc:Axis.Separator>
                                                        <lvc:Separator Stroke="{StaticResource BorderSoft}" StrokeThickness="1"/>
                                                    </lvc:Axis.Separator>
                                                </lvc:Axis>
                                            </lvc:CartesianChart.AxisY>
                                        </lvc:CartesianChart>
                                    </Grid>
                                </Border>
                            </Grid>

                            <!-- Row 2: 3D View Info (Text-based for now, can be enhanced with 3D chart later) -->
                            <Border Grid.Row="1" 
                                    Style="{StaticResource CardBorder}" 
                                    Padding="15">
                                <StackPanel>
                                    <TextBlock Text="3D Trajectory Summary" 
                                               FontSize="12" 
                                               FontWeight="SemiBold" 
                                               Foreground="{StaticResource TextStrong}"
                                               Margin="0,0,0,10"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Max TVD" FontSize="10" Foreground="{StaticResource TextMuted}"/>
                                            <TextBlock Text="{Binding MaxSurveyTVD, StringFormat='{}{0:F0} ft'}" 
                                                       FontSize="14" 
                                                       FontWeight="Bold" 
                                                       Foreground="{StaticResource BrandBlue}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="Max Horizontal Displacement" FontSize="10" Foreground="{StaticResource TextMuted}"/>
                                            <TextBlock Text="{Binding MaxSurveyVerticalSection, StringFormat='{}{0:F0} ft'}" 
                                                       FontSize="14" 
                                                       FontWeight="Bold" 
                                                       Foreground="{StaticResource BrandOrange}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Current Report MD" FontSize="10" Foreground="{StaticResource TextMuted}"/>
                                            <TextBlock Text="{Binding ReportMD, StringFormat='{}{0:F0} ft'}" 
                                                       FontSize="14" 
                                                       FontWeight="Bold" 
                                                       Foreground="Red"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </Grid>


                    </Grid>
                </ScrollViewer>
            </TabItem>

            <!-- ========================= -->
            <!-- Thermal Gradient -->
            <!-- ========================= -->
            <TabItem Header="Thermal Gradient">
                <Grid Margin="18">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Style="{StaticResource CardBorder}">
                        <geometry:ThermalGradientView DataContext="{Binding ThermalGradientViewModel}"/>
                    </Border>


                </Grid>
            </TabItem>

            <!-- ========================= -->
            <!-- Well Test -->
            <!-- ========================= -->
            <TabItem Header="Well Test">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="18">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <!-- Header & Button -->
                            <RowDefinition Height="Auto"/>
                            <!-- Spacer -->
                            <RowDefinition Height="*"/>
                            <!-- DataGrid -->
                        </Grid.RowDefinitions>

                        <!-- Header & Button -->
                        <Border Grid.Row="0" Style="{StaticResource CardBorder}" Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="Keep tests aligned to a section."
                                               Foreground="{StaticResource TextMuted}"
                                               FontStyle="Italic"
                                               FontSize="13"/>
                                </StackPanel>

                                <Button Grid.Column="1" 
                                        Content="Add Well Test" 
                                        Style="{StaticResource BrandPrimaryButton}"
                                        Command="{Binding AddWellTestCommand}"
                                        VerticalAlignment="Center"/>
                            </Grid>
                        </Border>

                        <!-- Well Test DataGrid -->
                        <DataGrid Grid.Row="2"
                                  x:Name="WellTestDataGrid"
                                  Style="{StaticResource ModernDataGrid}"
                                  ItemsSource="{Binding WellTests}"
                                  SelectedItem="{Binding SelectedWellTest}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="True"
                                  SelectionMode="Single"
                                  AlternationCount="2">

                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="White"/>
                                    <Style.Triggers>
                                        <Trigger Property="AlternationIndex" Value="1">
                                            <Setter Property="Background" Value="#FFF9FAFB"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#FFE0F2FE"/>
                                            <!-- Light Blue Selection -->
                                            <Setter Property="BorderBrush" Value="{StaticResource BrandBlue}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>

                            <DataGrid.Columns>
                                <!-- ID Column -->
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40" IsReadOnly="True" Foreground="{StaticResource TextMuted}"/>

                                <!-- Section Column (Dropdown) -->
                                <DataGridTemplateColumn Header="Section" Width="180">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Section, TargetNullValue='-- Select --'}" VerticalAlignment="Center" Padding="8,4"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding DataContext.WellboreSectionNames, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                      SelectedItem="{Binding Section, UpdateSourceTrigger=PropertyChanged}"
                                                      DisplayMemberPath="."
                                                      Padding="5"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>

                                <!-- Dropdown Test Type -->
                                <DataGridTemplateColumn Header="Dropdown Test Type" Width="180">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding TypeString}" VerticalAlignment="Center" Padding="8,4"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding DataContext.WellTestTypes, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                      SelectedItem="{Binding TypeString, UpdateSourceTrigger=PropertyChanged}"
                                                      Padding="5"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>

                                <!-- Test Value (ppb) -->
                                <DataGridTemplateColumn Header="Test Value (ppb)" Width="140">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding TestValue, StringFormat=0.##}" VerticalAlignment="Center" Padding="8,4" Foreground="{StaticResource TextStrong}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding TestValue, UpdateSourceTrigger=PropertyChanged, StringFormat=0.##}" Padding="5"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>

                                <!-- Actions -->
                                <DataGridTemplateColumn Header="Actions" Width="160">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Content="↑" Style="{StaticResource IconMiniButton}" ToolTip="Up"
                                                        Click="MoveWellTestUp_Click" Tag="{Binding}"/>
                                                <Button Content="↓" Style="{StaticResource IconMiniButton}" ToolTip="Down"
                                                        Click="MoveWellTestDown_Click" Tag="{Binding}"/>
                                                <Button Content="×" Style="{StaticResource IconMiniButton}" ToolTip="Delete"
                                                        Click="DeleteWellTest_Click" Tag="{Binding}" Foreground="{StaticResource Danger}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <!-- ========================= -->
            <!-- Summary (incluye VisualSchemeCanvas) -->
            <!-- ========================= -->
            <TabItem Header="Summary">
                <Grid Margin="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="320"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left: Visual Schematic Panel -->
                    <Border Grid.Column="0" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="0,0,1,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Padding="15,15,15,10" Background="White" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
                                <StackPanel>
                                    <TextBlock Text="VISUAL SCHEMATIC" FontWeight="Bold" Foreground="#4B5563" FontSize="11" Margin="0,0,0,4"/>
                                    <TextBlock Text="Wellbore &amp; Drill String" Foreground="{StaticResource TextMuted}" FontSize="12"/>
                                </StackPanel>
                            </Border>

                            <!-- Dynamic Schematic Canvas con scroll -->
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Margin="10">
                                <geometry:BhaSchematicView x:Name="MasterSchematic" MinWidth="200" MinHeight="600"/>
                            </ScrollViewer>

                            <Border Grid.Row="2" Padding="20" Background="White" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0">
                                <StackPanel>

                                    <Separator Margin="0,15"/>


                                    <Button Content="[Edit Geometry]" Command="{Binding EditGeometryCommand}" Style="{StaticResource BrandPrimaryButton}" Margin="0,0,0,8"/>
                                    <Button Content="[Edit Drill String]" Command="{Binding EditStringCommand}" Style="{StaticResource BrandPrimaryButton}"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Right: Dashboard Content -->
                    <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                        <Grid Margin="25">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <!-- Hero: Total System Volume -->
                                <RowDefinition Height="Auto"/>
                                <!-- Volumes Blocks -->
                                <RowDefinition Height="Auto"/>
                                <!-- Safety Metrics -->
                                <RowDefinition Height="Auto"/>
                                <!-- Annular Details Table -->
                                <RowDefinition Height="Auto"/>
                                <!-- Action Bar -->
                            </Grid.RowDefinitions>


                            <!-- Volume Blocks Row -->
                            <Grid Grid.Row="1" Margin="0,0,0,20">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <!-- Circulation Card -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- Breakdown Cards -->
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Circulation & Trip Volume Card -->
                                <GroupBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Header="Circulation &amp; Trip Volume" Style="{StaticResource StandardGroupBox}" Margin="0,0,0,20">
                                    <Grid Margin="10">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <!-- Inputs -->
                                            <RowDefinition Height="Auto"/>
                                            <!-- Table Header -->
                                            <RowDefinition Height="Auto"/>
                                            <!-- Rows -->
                                            <RowDefinition Height="Auto"/>
                                            <!-- Total -->
                                        </Grid.RowDefinitions>

                                        <!-- Table Structure (Headers) -->
                                        <Grid Grid.Row="1" Background="#F3F4F6" Margin="0,0,0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="1.5*"/>
                                                <ColumnDefinition Width="2.5*"/>
                                                <ColumnDefinition Width="1.5*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="Volume Section" FontWeight="Bold" Padding="8" Foreground="#4B5563"/>
                                            <TextBlock Grid.Column="1" Text="Engineering Calculation" FontWeight="Bold" Padding="8" Foreground="#4B5563"/>
                                            <TextBlock Grid.Column="2" Text="Volume (bbl)" FontWeight="Bold" Padding="8" HorizontalAlignment="Right" Foreground="#4B5563"/>
                                        </Grid>

                                        <!-- Rows -->
                                        <StackPanel Grid.Row="2">
                                            <!-- Internal String -->
                                            <Grid Margin="0,2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="1.5*"/>
                                                    <ColumnDefinition Width="2.5*"/>
                                                    <ColumnDefinition Width="1.5*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="Internal String" Padding="8,4" VerticalAlignment="Center"/>
                                                <TextBlock Grid.Column="1" Text="Capacity to Bit Depth" Padding="8,4" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                                                <TextBlock Grid.Column="2" Text="{Binding CalculatedInternalStringVolume, StringFormat=0.##}" Padding="8,4" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                                            </Grid>

                                            <Separator Margin="0,2" Background="#F3F4F6"/>

                                            <!-- Annular -->
                                            <Grid Margin="0,2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="1.5*"/>
                                                    <ColumnDefinition Width="2.5*"/>
                                                    <ColumnDefinition Width="1.5*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="Annular (Active)" Padding="8,4" VerticalAlignment="Center"/>
                                                <TextBlock Grid.Column="1" Text="Annulus to Bit Depth" Padding="8,4" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                                                <TextBlock Grid.Column="2" Text="{Binding CalculatedAnnularActiveVolume, StringFormat=0.##}" Padding="8,4" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                                            </Grid>

                                            <Separator Margin="0,2" Background="#F3F4F6"/>

                                            <!-- Open Hole -->
                                            <Grid Margin="0,2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="1.5*"/>
                                                    <ColumnDefinition Width="2.5*"/>
                                                    <ColumnDefinition Width="1.5*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="Open Hole (Below Bit)" Padding="8,4" VerticalAlignment="Center"/>
                                                <TextBlock Grid.Column="1" Text="Hole Capacity (Bit to TD)" Padding="8,4" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                                                <TextBlock Grid.Column="2" Text="{Binding CalculatedOpenHoleVolumeBelowBit, StringFormat=0.##}" Padding="8,4" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="{StaticResource BrandOrange}"/>
                                            </Grid>
                                        </StackPanel>

                                        <!-- Total -->
                                        <Border Grid.Row="3" Background="#ECFDF5" Margin="0,15,0,0" CornerRadius="6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="TOTAL WELL VOLUME" FontWeight="Bold" Padding="12" Foreground="#065F46" VerticalAlignment="Center"/>
                                                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="12">
                                                    <TextBlock Text="{Binding CalculatedTotalMudInSystem, StringFormat=0.##}" FontWeight="Bold" FontSize="18" Foreground="#065F46"/>
                                                    <TextBlock Text=" bbl" VerticalAlignment="Bottom" Margin="4,0,0,3" Foreground="#065F46"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>

                                    </Grid>
                                </GroupBox>



                                <!-- System Volume Percentages -->
                                <GroupBox Grid.Row="1" Grid.Column="2" Header="Distribution" Style="{StaticResource StandardGroupBox}">
                                    <StackPanel Margin="15" VerticalAlignment="Center">
                                        <Grid Margin="0,5">
                                            <TextBlock Text="Annular" Foreground="#4B5563"/>
                                            <TextBlock Text="{Binding AnnularVolumePercent, Mode=OneWay, StringFormat='{}{0:F1} %'}" HorizontalAlignment="Right" FontWeight="Bold"/>
                                        </Grid>
                                        <Grid Margin="0,5">
                                            <TextBlock Text="Drill String" Foreground="#4B5563"/>
                                            <TextBlock Text="{Binding StringVolumePercent, Mode=OneWay, StringFormat='{}{0:F1} %'}" HorizontalAlignment="Right" FontWeight="Bold"/>
                                        </Grid>
                                        <ProgressBar Value="{Binding AnnularVolumePercent, Mode=OneWay}" Height="10" Margin="0,10,0,0" Foreground="{StaticResource BrandBlue}"/>
                                    </StackPanel>
                                </GroupBox>
                            </Grid>

                            <!-- Safety Metrics Row -->
                            <Grid Grid.Row="2" Margin="0,0,0,20">
                                <GroupBox Header="Drill String Status" Style="{StaticResource StandardGroupBox}">
                                    <StackPanel Margin="15">
                                        <Grid Margin="0,5">
                                            <TextBlock Text="Bottom Status" Foreground="#4B5563"/>
                                            <TextBlock Text="{Binding DepthDifferentialStatus}" HorizontalAlignment="Right" FontWeight="Bold" Foreground="{Binding DepthDifferentialColor}"/>
                                        </Grid>
                                        <Grid Margin="0,5">
                                            <TextBlock Text="Bit Depth" Foreground="#4B5563"/>
                                            <TextBlock Text="{Binding TotalDrillStringLength, StringFormat='{}{0:F2} ft'}" HorizontalAlignment="Right" FontWeight="Bold"/>
                                        </Grid>
                                    </StackPanel>
                                </GroupBox>
                            </Grid>

                            <!-- Annular Volume Breakdown Table -->
                            <GroupBox Grid.Row="3" Header="Annular Volume Details" Style="{StaticResource StandardGroupBox}" Margin="0,0,0,20">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <DataGrid ItemsSource="{Binding Source={StaticResource AnnularDetailsView}}"
                                            Style="{StaticResource ModernDataGrid}"
                                            AutoGenerateColumns="False"
                                            IsReadOnly="True"
                                            MaxHeight="500"
                                            MinHeight="150"
                                            RowHeight="28"
                                            Margin="5"
                                            AlternationCount="2"
                                            GridLinesVisibility="Horizontal"
                                            VerticalScrollBarVisibility="Auto"
                                            HorizontalScrollBarVisibility="Auto">
                                        <DataGrid.GroupStyle>
                                            <GroupStyle>
                                                <GroupStyle.HeaderTemplate>
                                                    <DataTemplate>
                                                        <Border Background="#F3F4F6" Padding="8,4" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="Stage: " FontWeight="Bold" Foreground="#374151"/>
                                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#374151"/>
                                                                <TextBlock Text="{Binding ItemCount, StringFormat=' ({0} segments)'}" Foreground="#6B7280" Margin="10,0,0,0" FontSize="11" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Border>
                                                    </DataTemplate>
                                                </GroupStyle.HeaderTemplate>
                                            </GroupStyle>
                                        </DataGrid.GroupStyle>
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Segment" Binding="{Binding Name}" Width="1.2*" MinWidth="100"/>
                                            <DataGridTextColumn Header="From" Binding="{Binding TopMD, StringFormat=F0}" Width="0.7*" MinWidth="70">
                                                <DataGridTextColumn.ElementStyle>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                                        <Setter Property="Padding" Value="8,4"/>
                                                    </Style>
                                                </DataGridTextColumn.ElementStyle>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Header="To" Binding="{Binding BottomMD, StringFormat=F0}" Width="0.7*" MinWidth="70">
                                                <DataGridTextColumn.ElementStyle>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                                        <Setter Property="Padding" Value="8,4"/>
                                                    </Style>
                                                </DataGridTextColumn.ElementStyle>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Header="ID (in)" Binding="{Binding WellboreID, StringFormat=F3}" Width="0.8*" MinWidth="80">
                                                <DataGridTextColumn.ElementStyle>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                                        <Setter Property="Padding" Value="8,4"/>
                                                    </Style>
                                                </DataGridTextColumn.ElementStyle>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Header="OD (in)" Binding="{Binding DrillStringOD, StringFormat=F3}" Width="0.8*" MinWidth="80">
                                                <DataGridTextColumn.ElementStyle>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                                        <Setter Property="Padding" Value="8,4"/>
                                                    </Style>
                                                </DataGridTextColumn.ElementStyle>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Header="Cap. (bbl/ft)" Binding="{Binding Capacity, StringFormat=F5}" Width="1*" MinWidth="100">
                                                <DataGridTextColumn.ElementStyle>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                                        <Setter Property="Padding" Value="8,4"/>
                                                    </Style>
                                                </DataGridTextColumn.ElementStyle>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Header="Elements" Binding="{Binding ElementDescription}" Width="2*" MinWidth="150"/>
                                            <DataGridTextColumn Header="Vol. (bbl)" Binding="{Binding Volume, StringFormat=0.##}" Width="0.9*" MinWidth="90">
                                                <DataGridTextColumn.ElementStyle>
                                                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                                        <Setter Property="Foreground" Value="#059669"/>
                                                        <Setter Property="Padding" Value="8,4"/>
                                                    </Style>
                                                </DataGridTextColumn.ElementStyle>
                                            </DataGridTextColumn>
                                        </DataGrid.Columns>

                                    </DataGrid>

                                    <!-- Total Row -->
                                    <Border Grid.Row="1" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Margin="10,0,10,10" Padding="10,8">
                                        <Grid>
                                            <TextBlock Text="TOTAL" FontWeight="Bold" FontSize="13" Foreground="#1F2937"/>
                                            <TextBlock Text="{Binding TotalAnnularVolume, StringFormat='{}{0:F2} bbl'}" 
                                                   HorizontalAlignment="Right" 
                                                   FontWeight="Bold" 
                                                   FontSize="13"
                                                   Foreground="#059669"/>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </GroupBox>
                        </Grid>
                    </ScrollViewer>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
